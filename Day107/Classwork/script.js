// classwork 1:

// let arr = ['Goa', 'novatori', 'step', 'step'];

// let finish_arr = arr.map(item => item[0] === item[0].toUpperCase() ? 'Good' : 'Bad');
// console.log(finish_arr);

// classwork 2:

// let res = [11, 3, 1, 5, 6, 2, 0, 13].filter(num => num > 5);
// console.log(res);

// classwork 3:

let arr = ['Hello', '', 'world', '!'];

let con = arr.reduce((acc, curr) => acc += curr, '');
console.log(con);