// 1:

// let age = Number(prompt('Enter your age: '));
// return age >= 18 ? 'Adult' : age < 18 && age >= 0 ? 'Child' : NaN;

// 2:

// let num1 = Number(prompt('Enter the first number: '));
// let num2 = Number(prompt('Enter the second number: '));
// return num1 < num2 ? 'num2' : num1 > num2 ? 'num1' : num1 - num2 == 0 ? 'Equal' : NaN;

// 3:

// function fakeBin(x) {
//     let res = '';
  
//     for(let i = 0; i < x.length; i++) {
//       x[i] < 5 ? res += '0' : res += '1';
//     }
  
//     return res;
// }