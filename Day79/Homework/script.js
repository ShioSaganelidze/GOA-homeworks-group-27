// function isALeapYear(year) {
//     this.year = year;

//     if(this.year % 4 == 0 && this.year % 100 != 0 || this.year % 400 == 0) {
//         return `${this.year} - Yes`
//     } else {
//         return `${this.year} - No`
//     }
// };

// console.log(isALeapYear(2400));

// function threeDigitSum(num) {
//     if (num >= 100 && num <= 999) {
//         let numStr = num.toString();
//         return Number(numStr[0]) + Number(numStr[1]) + Number(numStr[2]);
//     } else {
//         return undefined;
//     }
// };

// console.log(threeDigitSum(937));