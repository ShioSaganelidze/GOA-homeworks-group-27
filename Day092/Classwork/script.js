// let num = Number(prompt('Enter a number: '))

// // return num % 2 === 0 ? 'Two' : `${num}`;
// // return num % 2 === 0 ? `${num} * 8 = ${num * 8}` : num % 2 !== 0 ? `${num} * 9 = ${num * 9}` : null;

// function checkNumber(nums) {
//     return nums < 0 ? 'Negative' : nums > 0 ? 'Positive' : 'Neither';
// }

// function randomName() {
//     let arr = ['sergi', 'andria', 'giorgi rodonaia', 'dachi', 'luka papashvili', 'nodo', 'shio', 'giorgi nozadze', 'nika datashvili', 'zaza', 'irakli', 'kote', 'luka chumbashvili', 'rezi', 'davit wiqrize', 'nika vanishvili', 'luka demetrashivili', 'luka chitidze', 'aleqsandre afxazava', 'nikoloz dvali'];
//     return arr[Math.floor(Math.random() * arr.length)];
// }

// console.log(randomName());